# T-006 Release Hygiene and Default Branch Alignment

## Goal

Confirm that the v0.5.0 tag, default branch, README, examples index, changelog,
and release notes present the same governance demo story after the v0.5.0
release.

## Background

v0.5.0 added the governance demo matrix and tagged the closeout commit. A later
raw README check appeared to show the default branch still listing only the AI
Drift Demo. T-006 verifies the repository and remote raw content before opening
the next feature task.

This task is release hygiene only. It does not add new functionality, retag
v0.5.0, or change guard, risk, adapter, closeout, installer, profile, or demo
script behavior.

## Plan Contract

- plan: `docs/plans/T-006-plan.md`
- current_step: completed

## Allowed Changes

- `docs/tasks/T-006.md`
- `docs/plans/T-006-plan.md`
- `CHANGELOG.md`
- `README.md` only if Markdown formatting is compressed or missing the four
  demo commands
- `examples/README.md` only if Markdown formatting is compressed or missing the
  four demo commands

## Forbidden Changes

- `core/scripts/**`
- `scripts/test-kit.sh`
- `installer/**`
- `profiles/**`
- `examples/*/run-demo.sh`
- `docs/policies/**`
- `.env`
- `.env.*`
- `*.pem`
- `*.key`
- `id_rsa`
- `id_ed25519`
- `target/**`
- `build/**`
- `dist/**`
- `node_modules/**`
- `.m2home/**`
- `.idea/**`

## Checks Recorded

Verified on 2026-07-06 after `git fetch --tags origin`:

- `HEAD` = `bd6f64428d0439c6130caebe365e5bb60d55f2f6`
- `origin/master` = `bd6f64428d0439c6130caebe365e5bb60d55f2f6`
- `v0.5.0` = `bd6f64428d0439c6130caebe365e5bb60d55f2f6`
- local branch containing `v0.5.0`: `master`
- remote branch containing `v0.5.0`: `origin/master`

Remote raw content was rechecked with cache-busting URLs:

- `master` README has 216 lines.
- `master` README lists all four demo commands.
- `v0.5.0` README lists all four demo commands.
- `master` `examples/README.md` has 10 lines and lists all four demos.

## Required Work

- Record the default branch and tag alignment check.
- Record that README and `examples/README.md` already list all four demos.
- Record that `docs/releases/v0.5.0.md` documents the release boundary and
  known limits.
- Add a v0.5.0 changelog entry if missing.

## Forbidden Actions

- Do not add a CLI.
- Do not add adapter sync.
- Do not change demo script behavior.
- Do not change command guard, drift guard, risk report, adapter block,
  closeout, installer, or profile semantics.
- Do not retag v0.5.0.

## Test Requirements

- `git diff --check`
- `bash -n scripts/test-kit.sh`
- `bash scripts/test-kit.sh`
- `git log --oneline --decorate -n 8`
- `git branch --contains v0.5.0`

## Completion Criteria

- T-006 documents that v0.5.0, `origin/master`, and local `HEAD` were aligned at
  `bd6f64428d0439c6130caebe365e5bb60d55f2f6` before T-006 edits.
- README and `examples/README.md` are confirmed to expose all four demos.
- `docs/releases/v0.5.0.md` is confirmed to document the demo release boundary.
- `CHANGELOG.md` includes the v0.5.0 governance demo release.
- Release hygiene changes pass the required checks.

## Risk

low
