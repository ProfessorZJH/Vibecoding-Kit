# Acceptance Gate

A task is complete only when:

- The task card requirements are implemented.
- Modified files are within the task's allowed scope.
- Tests and guard scripts pass.
- Project state and work log are updated when required.
- The task has a commit checkpoint or an explicit no-commit checkpoint.
